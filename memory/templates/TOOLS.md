# {AGENT_NAME} — TOOLS.md

Configured tools (enable each by setting the matching env var in `.env`):

- LLM providers: Claude CLI, Codex CLI, Gemini CLI, Ollama, Anthropic API, OpenAI API, Google Gemini API
- Channels: Telegram, Discord, Slack, WhatsApp, Matrix, Email (Gmail)
- Services: Notion, Google Calendar, Google Drive, GitHub, Stripe
- Media: ElevenLabs (TTS), Groq Whisper (STT), Replicate, fal.ai
- Memory: ChromaDB (vector), SQLite (structured)

Credentials live in `.env` only — never commit secrets.
